/** Attachment REST routes. STUB — owned by the attachments workstream. */
import type { FastifyInstance } from "fastify";
import type { AppContext } from "./app.js";

export function registerAttachmentRoutes(_app: FastifyInstance, _ctx: AppContext) {
  // TODO(attachments): POST /api/attachments, PUT /api/attachments/:id/content, GET /api/attachments/:id, GET /api/attachments/:id/content
}
